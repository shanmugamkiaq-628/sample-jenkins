pipeline {
    agent {
        label 'linux'
    }
 
    stages {
        stage('Test Agent') {
            steps {
                echo 'Running on Jenkins Agent'
            }
        }
 
        stage('Build') {
            steps {
                echo 'Build completed successfully'
            }
        }
    }
}
